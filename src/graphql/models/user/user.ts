import logging from "../../../lib/logging/logging.js";
import type { UserResolvers } from "../../../generated/graphql.js";

const logger = logging.getLogger();

const User: UserResolvers = {

};

export default User;
